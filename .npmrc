# This project intentionally mixes Angular 17 runtime with older build tooling
# (@angular-devkit/build-angular 16, @angular/cli 18), so npm's strict peer
# resolution would refuse a clean install. legacy-peer-deps keeps `npm install`,
# `npm ci` (host, Docker build, preview) all resolving the same committed tree.
legacy-peer-deps=true
